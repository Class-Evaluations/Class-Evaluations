@model Survey.ViewModels.EmailStats

@{
    ViewBag.Title = "sendWelcome";
}

<h2>sendWelcome</h2>

<fieldset>
    <legend>EmailStats</legend>

    <div class="display-label">Emails Sent</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.EmailCountSent)
    </div>

    <div class="display-label">No Email Adddress</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.NoEmailAdddress)
    </div>

    <div class="display-label">Email Privacy Flag Set</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.EmailPrivacyFlag)
    </div>
</fieldset>
<p>
    @Html.ActionLink("Edit", "Edit", new { /* id=Model.PrimaryKey */ }) |
    @Html.ActionLink("Back to List", "Index")
</p>
