@model IEnumerable<Survey.ViewModels.CourseStatusGroup>

@{
    ViewBag.Title = "Index";
}

<h2>Home</h2>

@*<h3>A status of S indicates that a survey has been sent for these courses, a status of X means these courses are excluded from having a survey sent.</h3>
<table>
    <tr>
        <th>
            Courses with Status
        </th>
        <th>
            Course Count
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CourseStatus)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CourseCount)
        </td>
    </tr>
}

</table>*@
